/* 
    SAMPLE 1 - What Is TypeScript?

     TypeScript version

*/

function greeter(person) {
    return "Hello, " + person + "!";
}

document.getElementById("content").innerHTML
     = greeter("DevEvening");

// note comments are removed here, they can be retained if desired

// there is also a compiler option to generate a minified version .min.js

